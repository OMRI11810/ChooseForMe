# Copy to .env / set in Vercel (git-ignored).

# Base URL of the deployed backend — required in production.
# Example: https://chooseforme-backend-production.up.railway.app
# (no trailing slash). Leave unset for local development; Vite then proxies
# /api to http://localhost:8000.
VITE_API_URL=